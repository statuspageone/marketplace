# Example Provider Template

Copy this folder to `connectors/<provider-slug>` and replace every placeholder value with provider-specific metadata.

Replace:

- provider name, slug, and documentation URL
- OAuth scopes and environment variable names
- webhook endpoint metadata and event names
- polling endpoint, cursor field, and mapping rules
- fixture payload content

Keep every replacement sanitized. Do not paste secrets, live IDs, or real customer data.
