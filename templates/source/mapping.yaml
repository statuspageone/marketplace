# Route mappings: external events → canonical StatuspageOne events
routes:
  - sourceEvent: incident.created
    canonicalRoute: incident.created
    eventType: incident
    description: Placeholder mapping for a provider incident create event.

# Field mappings: external fields → canonical StatuspageOne fields
fields:
  - sourcePath: data.id
    canonicalField: incident.id

  - sourcePath: data.title
    canonicalField: incident.title
