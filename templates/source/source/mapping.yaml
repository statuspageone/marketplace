events:
  - sourceEvent: incident.created
    description: Placeholder mapping for a provider incident create event.
    fieldMappings:
      provider:
        literal: example-provider
      provider_event_type:
        literal: incident.created
      status:
        literal: open
      severity:
        literal: high
      title:
        sourcePath: data.title
      resource_id:
        sourcePath: data.id
      occurred_at:
        sourcePath: data.createdAt
      description:
        sourcePath: data.summary
      source_url:
        sourcePath: data.url
