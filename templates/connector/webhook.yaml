# Webhook subscription endpoint
subscriptionUrl: https://api.example.com/webhooks

# Header containing webhook signature
signatureHeader: x-example-signature

# Events to subscribe to
events:
  - incident.created

# Path to fixture file containing example webhook payload
fixturePath: fixtures/webhook-event.json
